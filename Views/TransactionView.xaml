<UserControl x:Class="RichIZ.Views.TransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RichIZ.ViewModels">
    <UserControl.DataContext>
        <vm:TransactionViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0" Text="수입/지출 관리" Style="{StaticResource TitleStyle}"/>

        <!-- 입력 폼 -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 제목 -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="제목" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                </StackPanel>

                <!-- 금액 -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,10,10">
                    <TextBlock Text="금액" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                </StackPanel>

                <!-- 날짜 -->
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="10,0,0,10">
                    <TextBlock Text="날짜" Style="{StaticResource LabelStyle}"/>
                    <DatePicker SelectedDate="{Binding Date}" Height="35" FontSize="14"/>
                </StackPanel>

                <!-- 유형 -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="유형" Style="{StaticResource LabelStyle}"/>
                    <ComboBox SelectedItem="{Binding SelectedType}" Height="35" FontSize="14">
                        <ComboBox.Items>
                            <ComboBoxItem Content="수입" Tag="Income"/>
                            <ComboBoxItem Content="지출" Tag="Expense" IsSelected="True"/>
                        </ComboBox.Items>
                    </ComboBox>
                </StackPanel>

                <!-- 카테고리 -->
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,10,10">
                    <TextBlock Text="카테고리" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding Category}" Height="35" FontSize="14"/>
                </StackPanel>

                <!-- 설명 -->
                <StackPanel Grid.Row="1" Grid.Column="2" Margin="10,0,0,10">
                    <TextBlock Text="설명" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                </StackPanel>

                <!-- 버튼들 -->
                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="추가" Command="{Binding AddTransactionCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
                    <Button Content="삭제" Command="{Binding DeleteTransactionCommand}" Style="{StaticResource DangerButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 거래 목록 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="거래 내역" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="날짜" Binding="{Binding Date, StringFormat=yyyy-MM-dd}" Width="100"/>
                        <DataGridTextColumn Header="제목" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="유형" Binding="{Binding Type}" Width="80"/>
                        <DataGridTextColumn Header="카테고리" Binding="{Binding Category}" Width="100"/>
                        <DataGridTextColumn Header="금액" Binding="{Binding Amount, StringFormat='{}{0:N0}원'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Type}" Value="Income">
                                            <Setter Property="Foreground" Value="#27AE60"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Expense">
                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="설명" Binding="{Binding Description}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
